<Page
    x:Class="CSharp.UWP.App.Pages.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CSharp.UWP.App.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid
        x:Name="Grid"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="{ThemeResource SystemControlAcrylicWindowBrush}"
        BorderBrush="#FFFEAF04"
        BorderThickness="7"
        CornerRadius="12">

        <!--  Window Notch  -->
        <Border Style="{StaticResource BorderStyleNotch}">

            <TextBlock
                x:Name="tbkTitle"
                Margin="0,10,0,0"
                HorizontalAlignment="Center"
                FontFamily="Monsterrat"
                FontSize="18"
                FontWeight="Bold"
                Foreground="Black"
                Text="C# PROG32356 UWP" />


        </Border>

        <StackPanel Margin="0,70" Background="{ThemeResource SystemControlAcrylicWindowBrush}">
            <StackPanel Background="ForestGreen" Orientation="Horizontal">

                <Button
                    Name="ClickMeButton"
                    Width="171"
                    Height="57"
                    Margin="10"
                    Padding="6"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Background="{StaticResource PrimaryColor}"
                    Content="Click Me"
                    FontSize="22"
                    FontWeight="Bold"
                    Foreground="Black" />

            </StackPanel>

        </StackPanel>



    </Grid>
</Page>
